---
/**
 * Astro Preact Netilify CMS Sandbox - Static site.
 *
 * @module /layouts/About
 * @description The "About" Astro layout.
 */

// Imports
import Base from './Base.astro';
import About from '../containers/About.jsx';

// The content prop gives access to frontmatter in a layout
const { content } = Astro.props;
---
<Base title="About">
  <About content={content} client:load>
    <slot />
  </About>
</Base>