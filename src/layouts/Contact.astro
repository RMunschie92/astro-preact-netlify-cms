---
// Imports
import Base from './Base.astro';
import Contact from '../containers/Contact.jsx';

// The content prop gives access to frontmatter in a layout
const { content } = Astro.props;
---
<Base title="Contact">
  <Contact content={content} client:load />
</Base>